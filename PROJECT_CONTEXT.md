# BlockPython 项目交接说明

> 本文用于让新的 Codex 聊天或项目协作者快速理解当前系统。开始任何修改前，请先阅读本文和根目录 `README.md`，再检查实际代码与数据库状态。若本文与代码不一致，以当前代码、测试和用户最新要求为准。

## 1. 项目定位

BlockPython 是面向初中一年级学生的“积木到 Python”过渡学习平台，也是毕业论文实验组使用的正式系统。学生通常具备一定 Scratch 基础，但尚未系统学习 Python。平台通过 Blockly 积木、Python 代码、可视化程序场景和过程感知学习助手，帮助学生逐步建立积木结构与 Python 语法之间的对应关系。

系统的核心教学内容是：

1. 从 Python 目标程序理解语义，并搭建含义相同的积木。
2. 根据完整、只读的积木结构独立转写 Python。
3. 通过真实运行结果理解变量、输入、条件、循环与二维坐标。
4. 在拓展任务中解释思路、修改程序、运行验证并进行反思。

像素场景、小明角色、水果和动画属于程序状态的可视化反馈。它们用于解释代码执行结果，不是独立游戏，也不增加积分、排行榜、奖励或额外“舞台指令”。小明的位置只由 Python 中的 `x`、`y` 等基础变量变化驱动。

## 2. 当前正式版

- 正式源码目录：`D:\工作罢了\26春\选题吗\系统\platform`
- GitHub 仓库：`https://github.com/CJERZSEYX/blockpython`
- 默认分支：`master`
- 正式版基线提交：`57e0973b4ffdc9e3de90b38701b64a865dcf49e5`
- 课程版本：`experiment-v2.8.1-student-flow-fixes`
- Agent 策略版本：`icap-multilevel-profile-agent-v5`
- 当前只维护中文实验组正式版。旧中英文原型、演示任务和历史开发模块不属于正式源码。

后续工作应以这一版本为基线进行小修小改。不要恢复已经删除的旧任务管理、提示词管理、完成率看板、旧英文课程、李白诗词动画或自定义舞台积木。

## 3. 技术结构

```text
platform/
├─ backend/                 Node.js + Express + TypeScript 后端
│  ├─ src/config/           数据库初始化与连接
│  ├─ src/routes/           登录、任务、运行、对话、学习状态与教师接口
│  ├─ src/services/         运行器、诊断、Agent、学习证据与教师报告
│  ├─ src/tasks/            六个任务的版本化课程配置
│  └─ src/tests/            课程、接口、摘要与Agent验证
├─ frontend/                React + TypeScript + Vite 前端
│  ├─ src/components/       Blockly、任务阶段、场景、聊天和教师预览
│  ├─ src/pages/            登录、任务中心、教学页和教师页
│  ├─ src/services/         前后端接口与学习时间记录
│  └─ public/               品牌、像素角色、地图与水果资源
├─ Dockerfile
├─ docker-compose.yml
├─ README.md
└─ PROJECT_CONTEXT.md
```

主要依赖：React、Ant Design、Blockly、Zustand、Express、MySQL 和 Python 3。Blockly 积木会生成真实 Python；A 阶段生成代码和 C/I 阶段学生代码进入同一套受控运行与目标判定流程。

## 4. 六个学习任务

六个任务共安排八课时，任务 3 和任务 6 建议各使用两课时，其余任务各一课时。课时分配用于教学安排，不产生分数或总进度。

| 任务 | 主题 | 主要知识 | 可视化作用 |
| --- | --- | --- | --- |
| 1 | 顺序执行与输出 | `print()`、文本、从上到下执行 | 小明按程序顺序显示气泡 |
| 2 | 变量与坐标 | 变量赋值、读取、加法、`x/y` 坐标 | 变量变化带动小明移动并收集水果 |
| 3 | 输入与条件判断 | `input()`、文本比较、`if-else` | 输入“苹果”或“梨”后选择不同路线 |
| 4 | 循环与重复执行 | `for range()`、循环变量、往返变化 | 小明重复往返并逐次收集同格水果 |
| 5 | 循环中的条件 | 循环内判断、嵌套、每轮变量变化 | 根据条件结果形成折线路线 |
| 6 | 二维综合任务 | 变量、输入、条件、循环和坐标综合 | 根据输入决定水果收集顺序并完成完整路线 |

六个任务使用统一的果园地图和小明角色，但每个任务只显示与当前学习目标有关的物品与状态。任务 1 不展示水果和坐标。任务 3、6 使用系统内运行输入框，不使用额外的方向按钮或平台函数。

## 5. 四类学习活动

学生界面不显示 ICAP 字母和内部 Agent 角色名，但系统内部仍按四类活动组织日志与支持策略。

### 任务分解（P）

- 顶部说明本次要实现的效果和运行后应观察什么。
- 下方把大任务拆成可展开步骤。
- 步骤展示所需积木类别、与 Blockly 抽屉一致的颜色、Python 翻译规则和独立微型示例。
- 不展示本题完整答案。
- 学习助手进行简短讲解和理解引导；查看行为只表示学生接触过概念，不代表掌握。

### 积木练习（A）

- 学生阅读目标 Python，从空白工作区搭建等义积木。
- 抽屉展示课程范围内全部标准积木，不含自定义舞台积木。
- 积木生成真实 Python，系统检查连接、输入槽、嵌套、游离块及运行结果。
- 第一次同类失败只显示确定性系统反馈；重复问题或主动求助时，学习助手提供短提示并定位相关积木。
- A 正确后解锁 C。学生首次进入 C 后，A 的目标 Python 隐藏为“本阶段已完成”，最终积木保留为只读回看内容。

### 代码挑战（C）

- 左侧显示与 A 目标相同的完整、已连接、只读积木。
- 学生从空白开始转写 Python。
- 学习助手结合 A 阶段积木摘要、当前代码和 AST/运行诊断，区分概念困难、迁移困难和语法困难。
- 提示可以定位到代码行和检查方向，但不得输出或补全本题完整代码。
- C 正确后解锁 I。

### 拓展互动（I）

- 对所有学生保持相同的拓展任务目标和判定标准。
- 学生独立编写拓展程序，并与学习助手围绕真实过程互动。
- 对话遵循“回顾—解释—编写—运行—反思—总结”，每轮只推进一个问题。
- 学习助手读取本任务 P/A/C 摘要及相关跨任务学习证据，选择一至两个支持重点；不得复述运行流水、重复已回答问题或暴露画像标签。
- I 不设置完成标志。

阶段权限为：P、A 初始可进入；A 达标后解锁 C；C 达标后解锁 I。正误和解锁始终由确定性检查器决定，大模型不参与评分。

## 6. 运行与动画机制

- A 阶段运行 Blockly 生成的 Python；C/I 运行学生代码。
- 程序受到允许语法、时间、输出和资源限制。
- 统一结果类型包括结构无效、语法错误、运行时错误、目标不符、达到目标、超时和取消。
- “能够运行”只表示 Python 语法和运行过程有效；是否达到教学目标由任务检查器另行判断。
- 语法或积木结构错误不播放动画；运行时错误保留错误前状态；逻辑错误忠实显示实际路线。
- `x` 增减控制左右，`y` 增减控制上下，一个单位对应一个草格。
- 水果只在小明真实到达目标地格后消失。小明和水果均位于格子中心。
- 小明每到一个地格停顿约一秒；重复运行会先恢复初始状态，不播放倒退回原点的过程。
- 任务 1 的连续输出会保留足够阅读时间，并在气泡中依次显示。

## 7. 学习助手与摘要 Agent

系统采用职责分离：

1. **确定性系统**：负责作品解析、正误判断、错误诊断、知识点映射、阶段权限和证据记录。
2. **学生学习助手**：读取可信的当前作品、匹配当前作品的最近运行、诊断和压缩学习摘要，用适龄语言讲解、提示、追问和反思。
3. **后端摘要 Agent**：负责把阶段证据压缩为阶段摘要、任务画像、课程画像和学生学习小结；不直接与学生对话，也不参与正误判断。

学生学习助手与摘要 Agent 使用独立后端配置。真实密钥只保存在 `backend/.env`，不得写入源码、文档、日志或 Git。摘要模型不可用、超时或输出校验失败时，系统使用确定性模板回退，不影响运行、判定和阶段权限。

Agent 的重要约束：

- 当前作品优先于历史作品，旧快照、旧运行和过期回复不得描述为当前状态。
- 不对每次拖拽或按键调用模型。稳定快照、提交、运行、求助、阶段边界和有效停滞才可能触发处理。
- 重复点击由前端门闩和后端幂等机制抑制，不通过大模型提醒。
- 相同作品重复求助复用既有提示；作品有效变化后才允许生成新提示。
- 大模型回复必须经过事实、定位和答案泄露检查；不可信时重试一次，再失败则使用确定性提示。
- 面向学生的回复应简短，每次聚焦一个主要问题。

## 8. 过程数据与学习证据

平台收集具有教学意义的过程数据，不记录鼠标坐标、屏幕录像、逐键输入或拖拽过程中的无意义中间位置。

主要数据包括：

- 登录会话、任务打开、阶段进入/退出和有效学习时间。
- P 阶段实际查看过的步骤、积木卡片和概念。
- A 阶段稳定 Blockly XML、语义结构、生成代码、连接与嵌套诊断、提交结果。
- C/I 阶段稳定代码快照、代码差异、AST 结构、输入、输出、错误行和运行结果。
- 变量轨迹、条件分支、循环路径、坐标移动和水果收集结果。
- 主动求助、系统介入触发原因、提示等级、定位位置及后续是否修正。
- 学生与学习助手的全部有效对话。
- A 积木到 C Python 的结构迁移比较。
- 阶段摘要、单任务学习证据画像和跨任务学习证据画像。

学习证据画像只描述可追溯行为，不给学生贴性格、能力或成绩标签。学生不可见完整画像；教师可查看可读证据。一次成功不会直接判定稳定掌握，高等级提示后的成功也不算独立成功。

## 9. 学生学习小结

- C 首次达标后生成任务级学习小结。
- I 阶段产生有效运行、修正或反思后可以更新。
- 小结包含“已经做到”“继续关注”“下一步建议”三项，每项一句话。
- 小结只在任务选择界面的对应任务入口查看，不占用 I 阶段顶部空间。
- 不展示分数、错误次数、画像状态、内部代号、模型信息或标准答案。
- 摘要 Agent 负责语言整理；失败时使用确定性模板。

## 10. 教师端

教师端的主要意义是观察和导出学生学习过程，不承担课程管理或实时评分。

教师可以查看：

- 学生列表及按任务组织的学习报告。
- 各阶段有效学习时间、A/C目标达成情况、尝试和求助概览。
- A 阶段真实积木的只读可视化预览。
- C/I 代码、输入、输出、诊断和关键错误演变。
- 按任务和阶段分组的对话记录。
- 阶段摘要、任务画像、课程画像及对应的可读证据。
- CSV 教师摘要与 JSON 完整研究数据包。

教师日常界面不应显示 XML、原始 JSON、哈希、快照 ID、请求 ID、会话 UUID、提示词版本、积木内部 ID或英文知识点代号。这些技术字段可以保留在完整研究导出中，用于论文审计。

## 11. 学习现场与恢复

- 数据库按“学生 + 任务”保存 A 积木、C 代码、I 代码、A/C完成状态和最新快照。
- 草稿在停止编辑约 1.5 秒后保存，提交、运行、切换阶段或离开任务时立即保存。
- 学生登录后先进入任务选择界面；点击任务后默认从任务分解阶段展示。
- 保存的数据、阶段权限和历史对话仍会恢复，因此默认展示 P 与保存学习现场并不冲突。
- 不恢复滚动位置、光标位置、步骤展开状态或动画播放进度。

## 12. UI 与产品约束

- 产品名称统一为 **BlockPython**，使用当前蓝紫渐变圆体字标和透明背景品牌图标。
- 学生端主要适配桌面，最低正式宽度为 1280px，保持“积木区—任务区—学习助手”三栏结构。
- 学生界面不展示 P/A/C/I 字母、ICAP术语或内部Agent角色名，右侧统一称为“学习助手”。
- Blockly 抽屉展示课程全部标准积木；P 工作区空白只读，A 可编辑，C/I 展示完整只读积木。
- A 的重置清空工作区；I 的重置恢复 C 参考积木并居中；C 参考积木不可编辑。
- 不引入自定义移动、拾取、说话等舞台积木。所有可视化行为必须来自基础 Python 变量、输入、输出、条件和循环。
- 页面中的说明应面向初一学生，避免开发者术语、冗长文字、内部状态和不必要表格。

## 13. 数据与安全

- `backend/.env`、API密钥、数据库内容、学生数据、日志、备份和构建产物均被 Git 忽略。
- `backend/.env.example` 只列配置项，不包含真实凭据。
- 不要在聊天回复、提交记录、测试代码或截图中重复用户提供过的真实密钥。
- 自动化测试创建的测试账号和数据必须在测试结束后清理。
- 删除数据库、历史回溯包或正式学习数据前必须获得用户明确确认。

## 14. 本地运行与验证

环境要求：Node.js 20+、MySQL 8、Python 3。

```powershell
# 后端
cd D:\工作罢了\26春\选题吗\系统\platform\backend
npm.cmd install
npm.cmd run dev

# 前端
cd D:\工作罢了\26春\选题吗\系统\platform\frontend
npm.cmd install
npm.cmd run dev
```

默认地址：

- 学生与教师前端：`http://127.0.0.1:5173`
- 后端健康检查：`http://127.0.0.1:3001/api/health`

每次正式修改后至少执行：

```powershell
cd D:\工作罢了\26春\选题吗\系统\platform\backend
npm.cmd run build
npm.cmd run test:validation
npm.cmd run test:api
npm.cmd run test:summary

cd D:\工作罢了\26春\选题吗\系统\platform\frontend
npm.cmd run lint
npm.cmd run build
npm.cmd run test:block-connections
npm.cmd run test:stage-feedback
```

涉及布局、Blockly、动画、阶段权限、输入流程或Agent交互时，还应在浏览器中回归任务 1、任务 3、任务 6，并检查 `1920×1080`、`1440×900`、`1366×768` 和 `1280×720`。

## 15. 后续修改原则

1. 先理解现有实现和教学目标，再修改代码。
2. 默认只做中文正式版的小修小改，不重新设计课程主线。
3. 不改变六个任务的 A/C 同目标原则，除非用户明确要求。
4. 不让大模型参与评分、目标判定或阶段解锁。
5. 不恢复积分、排名、总完成率或教师任务编辑功能。
6. 新反馈必须能对应当前积木、当前代码或当前运行证据。
7. 面向学生的文字尽量短、具体、适龄；面向教师的信息必须教学上可解释。
8. 修改前检查 Git 状态，不覆盖用户尚未提交的改动。
9. 修改后运行相关测试并进行学生视角浏览器回归。
10. 提交前扫描密钥、数据库、日志、测试账号和生成文件，确保不会进入 Git。

## 16. 新聊天建议的起始检查

新的聊天接手后，建议依次完成：

1. 阅读本文件和 `README.md`。
2. 查看 `git status --short --branch` 与最近提交，确认工作区状态。
3. 查看 `backend/src/tasks/curriculum.ts`，理解当前六个任务配置。
4. 根据问题定位到学生端、Agent、运行器、学习证据或教师端所属模块。
5. 若用户要求“讨论”，先讨论并等待确认；若用户明确要求“修改”，完成实现、验证和浏览器检查。

本项目已经经历多轮教学逻辑、UI、动画、Agent、数据记录和教师报告重构。当前正式版的重点是稳定、可解释和可用于毕业论文实验，后续修改应优先保护这一点。
